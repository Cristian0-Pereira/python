m = float(input('Qual sua nota de Matemática? '))
p = float(input('Sua nota de Português? '))
print(f'Sua média foi {(m + p) / 2:.1f}')
# ou coloca a variável para guardar na memoria.
# md = (m + p) / 2:.1f
